{
  "name": "inspire",
  "version": "1.0.0",
  "description": "Inspire is an experimental creativity app that fuels musicians with curated randomness — memes, words, sounds, and moods — to spark new ideas.",
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "build": "npm run build:backend && npm run build:frontend",
    "start:backend": "cd backend && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["creativity", "music", "inspiration", "typescript", "fullstack"],
  "author": "",
  "license": "ISC"
}
